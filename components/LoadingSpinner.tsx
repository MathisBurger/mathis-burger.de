import IntImg from './IntImg';

/**
 * The loading spinner is just a svg
 * that is displayed
 */
const LoadingSpinner = () => {
  return <IntImg src="/tail-spin.svg" />;
};

export default LoadingSpinner;
